#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    bool n;
    cin >> n;
    cout << (n ? "Leading the Way to the Future" : "YONSEI");
}
